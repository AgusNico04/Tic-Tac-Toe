export default class AI {
    constructor(canvas, board, difficulty, shape) {
        this.canvas = canvas;
        this.board = board;
        this.difficulty = difficulty;
        this.shape = shape;
    }

    
}